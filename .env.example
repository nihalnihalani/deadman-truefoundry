# Real mode (TrueFoundry AI Gateway + MCP Gateway + AWS Bedrock). The mock demo
# needs none of these — `python scripts/run_demo.py` runs on the stdlib.
TFY_API_KEY=
TFY_GATEWAY_BASE_URL=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
DEADMAN_MODE=mock
