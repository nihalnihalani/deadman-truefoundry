# The mock demo runs on the Python stdlib — no install needed:
#     python scripts/run_demo.py
#
# Install these for the web server + real mode
# (TrueFoundry AI Gateway + MCP Gateway + Bedrock):

# Web server
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0

# SSE (Server-Sent Events for /api/demo/stream)
sse-starlette>=1.6

# HTTP client (real-mode MCP calls + healthcheck)
requests>=2.31

# Real-mode AI Gateway (OpenAI-compatible TFY endpoint)
openai>=1.30

# Real-mode + DynamoDB state backend
boto3>=1.34

# Observability — Prometheus /metrics (degrades to no-op stubs if absent)
prometheus-client>=0.20

# Testing
pytest>=8.0

# ---------------------------------------------------------------
# OPTIONAL: OpenTelemetry — comment out to keep the image slim.
# Enable by uncommenting and rebuilding when OTEL_EXPORTER_OTLP_ENDPOINT is set.
# ---------------------------------------------------------------
# opentelemetry-sdk>=1.24
# opentelemetry-exporter-otlp-proto-grpc>=1.24
# opentelemetry-instrumentation-fastapi>=0.45b0
