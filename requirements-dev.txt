# Development + CI tooling dependencies
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Linting + formatting
ruff>=0.15.15

# Type checking
mypy>=1.9.0
types-requests>=2.33.0.20260518    # stubs for the `requests` library used in realmode_mcp

# Testing
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27.0             # powers FastAPI TestClient (starlette dependency)

# Security scanning
pip-audit>=2.7.0
bandit>=1.7.8

# Pre-commit hooks runner
pre-commit>=3.7.0

# Integration testing — DynamoDB in-memory mock (used by test_dynamodb_integration.py)
moto[dynamodb]>=5.2.2
